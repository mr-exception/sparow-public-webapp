name: sparow-public-webapp
services:
  sparow-public-webapp:
    public: true
    image: okteto.dev/sparow-public-webapp
    replicas: 1
    ports:
      - 5000
    resources:
      cpu: 100m
      memory: 128Mi